import _ from "lodash";
import { useState } from "react";
const Authenticate = ({ text = "", highlight = "" }) => {
  const [authenticated, setAuthenticated] = useState(false);
  return (
  );
};
export default Authenticate;
